name: Docs

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Print CPU diagnostics
        uses: ./.github/actions/cpu-diagnostics

      - name: Setup micromamba
        uses: mamba-org/setup-micromamba@f457c30a868e4760d3a6fcea5f25dc655b8edf39 # v3
        with:
          environment-file: environment-cpu.yml
          environment-name: skala
          # Recreate native environments per runner to avoid restoring CPU-specific binaries.
          cache-environment: false
          cache-downloads: true
          create-args: >-
            python=3.12

      - name: Install package
        run: |
          pip install -e '.[doc]'
        shell: micromamba-shell {0}

      - name: Check environment consistency
        run: |
          pip check
        shell: micromamba-shell {0}

      - name: Build documentation
        run: |
          sphinx-build -b html docs docs/_build/html
          touch docs/_build/html/.nojekyll
        shell: micromamba-shell {0}

      - name: Check external links
        run: |
          # The HTML build above executes notebooks; linkcheck only needs their links.
          sphinx-build -D nb_execution_mode=off -b linkcheck docs docs/_build/linkcheck
        shell: micromamba-shell {0}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: docs/_build/html

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Print CPU diagnostics
        uses: ./.github/actions/cpu-diagnostics

      - name: Deploy to GitHub Pages
        id: deploy
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
